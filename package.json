{
  "scripts": {
    "install:all": "npm run install:front && npm run install:admin && npm run install:server",
    "install:front": "cd frontend && npm install && cd ../",
    "install:admin": "cd admin && npm install && cd ../",
    "install:server": "cd backend && npm install && cd ../",
    "client": "cd frontend && npm run dev",
    "admin": "cd admin && npm run dev",
    "server": "cd backend && npm run server"
  }
}
